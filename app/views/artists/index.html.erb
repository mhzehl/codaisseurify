<h1>Artist Overview</h1>

<ul>
  <% @artists.each do |artist| %>
    <li>
      <h4><%= link_to artist.name, artist_path(artist) %></h4>
      <p><%= image_tag artist.image_url %>
      <p><%= artist.genre %></p>
    </li>
  <% end %>
</ul>
